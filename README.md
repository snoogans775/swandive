# WASM with Go

## To build the Go code
```
cd ./src/wasm
make build
```
## To edit the HTML template
`cd ./assets`
Edit the index.html file
## To serve the page on port 9090
```
cd ./src/server
go run main.go
```
